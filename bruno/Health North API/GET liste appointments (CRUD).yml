info:
  name: GET liste appointments (CRUD)
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/appointments/api/appointments/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
