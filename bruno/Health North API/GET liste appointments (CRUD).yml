info:
  name: GET liste appointments (CRUD)
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/appointments/api/appointments/"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body[0].specialist
      operator: eq
      value: Dr. Martin

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
